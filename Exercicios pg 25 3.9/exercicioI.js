alert("Quadrado da diferença");

const valor1 = parseInt(prompt("Digite o primeiro valor"));
const valor2 = parseInt(prompt("Digite o segundo valor"));

const diferenca = valor1 - valor2;
const quadrado = diferenca * diferenca;

alert(`O quadrado da diferença entre ${valor1} e ${valor2} é ${quadrado}`);
